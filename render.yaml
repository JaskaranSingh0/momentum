# Render Infrastructure as Code
# Docs: https://render.com/docs/infrastructure-as-code

services:
  - type: web
    name: momentum
    env: node
    rootDir: server
    plan: free
    autoDeploy: true
    buildCommand: |
      npm install
      cd ../client
      npm install --include=dev
      npm run build
      cd ../server
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: NODE_ENV
        value: production
      # Ensure devDependencies are available during build steps
      - key: NPM_CONFIG_PRODUCTION
        value: "false"
      - key: CLIENT_URL
        value: self
      - key: GOOGLE_CALLBACK_URL
        value: https://momentum.onrender.com/auth/google/callback
      # Secrets to set in dashboard (not committed):
      # MONGO_URI, SESSION_SECRET, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, COOKIE_SECURE (true/false), COOKIE_SAMESITE (optional)
